###1.前后台框架  串口中断接收数据  在主循环中处理 
主循环中有其他任务，可能阻塞串口数据处理。
可能存在阻塞的模块： 
				显示屏
不存在阻塞的模块：
				数字输入、PWM输出、数字输出、模拟输出、红外模块、超声波模块、MPU6050、dht11（需验证）、ds18b20(阻塞较小，应该可以忽略)、灯条（需测试）。
				由于DHT11对时序的要求，为防止单总线阻塞串口处理，将串口处理放在定时中断中处理，次级优先级。
离线模式的业务需完善的的模块：
							MPU6050。
							dht11是否把湿度放前面。
							红外发送
							显示屏
逻辑量模式：
			触发是1，未触发是0
			
需添加的协议：LED显示模式
             上报板子型号
			 
MPU6050  上报数据量的问题。